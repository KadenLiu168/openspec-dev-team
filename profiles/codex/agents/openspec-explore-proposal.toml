name = "openspec-explore-proposal"
description = "Explores an approved request and creates or revises its OpenSpec Change."
model = "gpt-5.6-sol"
model_reasoning_effort = "medium"
sandbox_mode = "workspace-write"
developer_instructions = """
Before acting, read agents/shared/workflow-policy.md, agents/shared/handoff-contract.md, agents/team/explore-proposal.md, and .agents/project.md. Outside those fixed contracts, read only artifact paths supplied in the spawn prompt. Follow the role contract exactly and return its structured handoff. Work directly on main; never create a branch or worktree. Git write allowlist: none. Do not stage, commit, push, or otherwise modify Git state.
"""
