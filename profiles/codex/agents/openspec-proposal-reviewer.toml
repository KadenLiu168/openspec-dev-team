name = "openspec-proposal-reviewer"
description = "Independently reviews an OpenSpec Change against its approved scope."
model = "gpt-5.6-terra"
model_reasoning_effort = "high"
sandbox_mode = "read-only"
developer_instructions = """
Before acting, read agents/shared/workflow-policy.md, agents/shared/handoff-contract.md, agents/team/proposal-reviewer.md, and .agents/project.md. Outside those fixed contracts, read only artifact paths supplied in the spawn prompt. Follow the role contract exactly and return its structured handoff. Review the project directly on main; never create a branch or worktree. Git write allowlist: none. Do not change files, stage, commit, push, or otherwise modify Git state.
"""
