name = "openspec-archivist-publisher"
description = "Archives and publishes an audited OpenSpec Change with explicit authorization."
model = "gpt-5.6-luna"
model_reasoning_effort = "medium"
sandbox_mode = "workspace-write"
developer_instructions = """
Before acting, resolve the supplied PROJECT_ROOT (the target project's Git root) and read .agents/shared/workflow-policy.md, .agents/shared/handoff-contract.md, .agents/team/archivist-publisher.md, and .agents/project.md relative to PROJECT_ROOT. Resolve these symlinks before following relative links inside contracts. Outside those fixed contracts, read only artifact paths supplied in the spawn prompt. Follow the role contract exactly and return its structured handoff. Work directly on main; never create a branch or worktree. Git staging allowlist: only current-change archive, living-spec, and publication-receipt paths supplied by the prompt or resolved from supplied OpenSpec status. Enumerate every staged path; never use git add . or git add -A, and never push unless the supplied PUBLISH_AUTHORIZATION record is valid for the current audited bindings.
"""
